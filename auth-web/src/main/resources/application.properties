security.oauth2.client.clientId=acme
security.oauth2.client.clientSecret=acmesecret
security.oauth2.client.authorized-grant-types=authorization_code,refresh_token,password
security.oauth2.client.scope=openid

# Default	
#security.user.name=user # Default user name.
#security.user.role=USER # Granted roles for the default user name.
security.user.password=password

# Enable when running at local environment
#server.port=7171
#server.context-path=/gundam-auth

# =======
# = Log =
# =======
# The following works with a Tomcat container.
stdout.appender=<appender-ref ref="STDOUT" />

# Comment out for Google App Engine
file.appender=<appender-ref ref="FILE_ROOT" />
log.path=logs
